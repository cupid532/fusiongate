name: fusiongate

services:
  fusiongate:
    build:
      context: ${FUSIONGATE_SOURCE_PATH}
      dockerfile: Dockerfile
    image: fusiongate:production
    restart: unless-stopped
    env_file:
      - ${FUSIONGATE_ENV_FILE}
    environment:
      FUSIONGATE_ADDR: 0.0.0.0:8787
      FUSIONGATE_DATA_DIR: /data
      FUSIONGATE_QUALITY_DETECTOR_URL: http://127.0.0.1:18789
      FUSIONGATE_QUALITY_DETECTOR_BASE_URL: http://127.0.0.1:8787/v1
      FUSIONGATE_MASTER_KEY_FILE: /run/secrets/fusiongate_master_key
      FUSIONGATE_ADMIN_PASSWORD_FILE: /run/secrets/fusiongate_admin_password
    expose:
      - "8787"
    volumes:
      - ${FUSIONGATE_DATA_PATH:-../../data}:/data
    read_only: true
    tmpfs:
      - /tmp:size=64m,noexec,nosuid,nodev
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    mem_limit: 768m
    mem_reservation: 256m
    cpus: 2.0
    pids_limit: 256
    secrets:
      - fusiongate_master_key
      - fusiongate_admin_password
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8787/readyz"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 15s
    networks:
      - internal

  quality-detector:
    build:
      context: ${FUSIONGATE_SOURCE_PATH}
      dockerfile: deploy/quality-detector.Dockerfile
    image: fusiongate-quality-detector:4.0.1
    restart: unless-stopped
    network_mode: service:fusiongate
    depends_on:
      fusiongate:
        condition: service_started
    volumes:
      - ${FUSIONGATE_QUALITY_DETECTOR_DATA_PATH:-../../quality-detector-data}:/runs
    read_only: true
    tmpfs:
      - /tmp:size=64m,noexec,nosuid,nodev
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    mem_limit: 1g
    mem_reservation: 256m
    cpus: 2.0
    pids_limit: 256

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    depends_on:
      fusiongate:
        condition: service_healthy
    environment:
      FUSIONGATE_DOMAIN: ${FUSIONGATE_DOMAIN}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ${FUSIONGATE_CADDYFILE_PATH}:/etc/caddy/Caddyfile:ro
      - ${CADDY_DATA_PATH:-../../caddy-data}:/data
      - ${CADDY_CONFIG_PATH:-../../caddy-config}:/config
    cap_drop:
      - ALL
    cap_add:
      - NET_BIND_SERVICE
    security_opt:
      - no-new-privileges:true
    mem_limit: 256m
    cpus: 1.0
    pids_limit: 128
    networks:
      - internal

networks:
  internal:
    driver: bridge

secrets:
  fusiongate_master_key:
    file: ${FUSIONGATE_MASTER_KEY_PATH}
  fusiongate_admin_password:
    file: ${FUSIONGATE_ADMIN_PASSWORD_PATH}
